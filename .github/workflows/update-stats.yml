# name: Update GitHub Stats
# on:
#   schedule:
#     # Runs at 12:00 AM UTC every day
#     - cron: '0 0 * * *'  # Fixed: correct 5-field cron expression
#   workflow_dispatch: # Allow manual trigger
#   push:
#     branches: [ main, master ]

# jobs:
#   update-readme:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
#       with:
#         token: ${{ secrets.GH_TOKEN }}
#         fetch-depth: 0

#     - name: Generate Snake Game
#       uses: Platane/snk@v3
#       with:
#         github_user_name: ${{ github.repository_owner }}
#         outputs: |
#           dist/github-contribution-grid-snake.svg
#           dist/github-contribution-grid-snake-dark.svg?palette=github-dark
#         github_token: ${{ secrets.GH_TOKEN }}

#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '18'

#     - name: Update README with Stats
#       run: |
#         # Create a script to update README with latest timestamps and force refresh
#         cat > update_readme.js << 'EOF'
#         const fs = require('fs');
        
#         // Read current README
#         let readme = fs.readFileSync('README.md', 'utf8');
        
#         // Add cache-busting query parameter to force refresh of stats
#         const timestamp = Date.now();
        
#         // Update GitHub Stats with enhanced parameters for private repos
#         readme = readme.replace(
#           /github-readme-stats\.vercel\.app\/api\?username=Entue250[^"]*"/g,
#           `github-readme-stats.vercel.app/api?username=Entue250&count_private=true&include_all_commits=true&show_owner=true&show_icons=true&theme=radical&hide_border=true&bg_color=161b22&title_color=f75c7e&text_color=c9d1d9&icon_color=58a6ff&ring_color=f75c7e&cache_bust=${timestamp}"`
#         );
        
#         // Update Top Languages with enhanced parameters
#         readme = readme.replace(
#           /github-readme-stats\.vercel\.app\/api\/top-langs\/\?username=Entue250[^"]*"/g,
#           `github-readme-stats.vercel.app/api/top-langs/?username=Entue250&count_private=true&include_all_commits=true&layout=compact&hide_border=true&theme=radical&bg_color=161b22&title_color=f75c7e&text_color=c9d1d9&cache_bust=${timestamp}"`
#         );
        
#         // Update GitHub Trophies
#         readme = readme.replace(
#           /github-profile-trophy\.vercel\.app\/\?username=Entue250[^"]*"/g,
#           `github-profile-trophy.vercel.app/?username=Entue250&theme=radical&no-frame=true&no-bg=false&margin-w=4&margin-h=15&column=8&row=1&title=Commits,Repositories,Stars,Followers,Experience,Issues,PullRequest,Reviews&cache_bust=${timestamp}"`
#         );
        
#         // Update Streak Stats
#         readme = readme.replace(
#           /github-readme-streak-stats\.herokuapp\.com\/\?user=Entue250[^"]*"/g,
#           `github-readme-streak-stats.herokuapp.com/?user=Entue250&theme=radical&hide_border=true&background=161b22&stroke=f75c7e&ring=f75c7e&fire=f75c7e&currStreakLabel=f75c7e&include_all_commits=true&cache_bust=${timestamp}"`
#         );
        
#         // Update Activity Graph
#         readme = readme.replace(
#           /github-readme-activity-graph\.vercel\.app\/graph\?username=Entue250[^"]*"/g,
#           `github-readme-activity-graph.vercel.app/graph?username=Entue250&theme=react-dark&cache_bust=${timestamp}"`
#         );
        
#         // Write updated README
#         fs.writeFileSync('README.md', readme);
#         console.log('README updated successfully with timestamp:', timestamp);
#         console.log('Private repository parameters included in stats URLs');
#         EOF
        
#         node update_readme.js

#     - name: Commit and push if changed
#       run: |
#         git config --local user.email "action@github.com"
#         git config --local user.name "GitHub Action"
#         git add .
#         git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update GitHub stats [skip ci]" && git push)


name: Update GitHub Stats and Profile
on:
  schedule:
    # Runs at 12:00 AM UTC every day
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger
  push:
    branches: [ main, master ]

jobs:
  update-profile:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GH_TOKEN }}
        fetch-depth: 0

    - name: Generate Snake Game
      uses: Platane/snk@v3
      with:
        github_user_name: ${{ github.repository_owner }}
        outputs: |
          dist/github-contribution-grid-snake.svg
          dist/github-contribution-grid-snake-dark.svg?palette=github-dark
        github_token: ${{ secrets.GH_TOKEN }}

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Update README with Enhanced Stats
      run: |
        cat > update_readme.js << 'EOF'
        const fs = require('fs');
        
        // Read current README
        let readme = fs.readFileSync('README.md', 'utf8');
        
        // Generate timestamp for cache busting
        const timestamp = Date.now();
        console.log('Updating README with timestamp:', timestamp);
        
        // Enhanced GitHub Stats with private repos
        readme = readme.replace(
          /github-readme-stats\.vercel\.app\/api\?username=Entue250[^"]*"/g,
          `github-readme-stats.vercel.app/api?username=Entue250&count_private=true&include_all_commits=true&show_owner=true&show_icons=true&theme=radical&hide_border=true&bg_color=161b22&title_color=f75c7e&text_color=c9d1d9&icon_color=58a6ff&ring_color=f75c7e&custom_title=Entue250's GitHub Stats (All Repos)&cache_bust=${timestamp}"`
        );
        
        // Enhanced Top Languages with private repos
        readme = readme.replace(
          /github-readme-stats\.vercel\.app\/api\/top-langs\/\?username=Entue250[^"]*"/g,
          `github-readme-stats.vercel.app/api/top-langs/?username=Entue250&count_private=true&include_all_commits=true&hide=html,css,scss&layout=compact&hide_border=true&theme=radical&bg_color=161b22&title_color=f75c7e&text_color=c9d1d9&langs_count=8&cache_bust=${timestamp}"`
        );
        
        // Update GitHub Trophies with enhanced configuration
        readme = readme.replace(
          /github-profile-trophy\.vercel\.app\/\?username=Entue250[^"]*"/g,
          `github-profile-trophy.vercel.app/?username=Entue250&theme=radical&no-frame=true&no-bg=false&margin-w=4&margin-h=15&column=8&row=1&title=Commits,Repositories,Stars,Followers,Experience,Issues,PullRequest,Reviews&rank=SECRET,SSS,SS,S,AAA,AA,A,B,C&cache_bust=${timestamp}"`
        );
        
        // Enhanced Streak Stats
        readme = readme.replace(
          /github-readme-streak-stats\.herokuapp\.com\/\?user=Entue250[^"]*"/g,
          `github-readme-streak-stats.herokuapp.com/?user=Entue250&theme=radical&hide_border=true&background=161b22&stroke=f75c7e&ring=f75c7e&fire=f75c7e&currStreakLabel=f75c7e&sideLabels=c9d1d9&currStreakNum=c9d1d9&sideNums=c9d1d9&dates=8b949e&excludeDaysLabel=8b949e&cache_bust=${timestamp}"`
        );
        
        // Enhanced Activity Graph
        readme = readme.replace(
          /github-readme-activity-graph\.vercel\.app\/graph\?username=Entue250[^"]*"/g,
          `github-readme-activity-graph.vercel.app/graph?username=Entue250&theme=react-dark&bg_color=161b22&color=c9d1d9&line=f75c7e&point=58a6ff&area=true&hide_border=true&custom_title=Contribution Activity Graph&cache_bust=${timestamp}"`
        );
        
        // Update individual stat components as well
        readme = readme.replace(
          /api\?username=Entue250&count_private=true&include_all_commits=true&show_icons=false[^"]*"/g,
          `api?username=Entue250&count_private=true&include_all_commits=true&show_icons=false&theme=radical&hide_border=true&cache_bust=${timestamp}"`
        );
        
        // Update last updated timestamp in footer
        const lastUpdated = new Date().toISOString().split('T')[0];
        readme = readme.replace(
          /Last updated: [^|]*/g,
          `Last updated: ${lastUpdated}`
        );
        
        // Write updated README
        fs.writeFileSync('README.md', readme);
        console.log('✅ README successfully updated!');
        console.log('✅ All GitHub stats now include private repository data');
        console.log('✅ Cache-busting timestamp applied:', timestamp);
        EOF
        
        node update_readme.js

    - name: Verify Private Repository Access
      run: |
        cat > verify_access.js << 'EOF'
        const https = require('https');
        
        const options = {
          hostname: 'api.github.com',
          path: '/user/repos?type=all&per_page=5',
          method: 'GET',
          headers: {
            'Authorization': `token ${process.env.GITHUB_TOKEN}`,
            'User-Agent': 'GitHub-Profile-Updater',
            'Accept': 'application/vnd.github.v3+json'
          }
        };
        
        const req = https.request(options, (res) => {
          let data = '';
          res.on('data', (chunk) => data += chunk);
          res.on('end', () => {
            if (res.statusCode === 200) {
              const repos = JSON.parse(data);
              const privateCount = repos.filter(repo => repo.private).length;
              const publicCount = repos.filter(repo => !repo.private).length;
              console.log('✅ GitHub API Access Verified');
              console.log(`📊 Found ${privateCount} private repos and ${publicCount} public repos in sample`);
              console.log('✅ Private repository access is working correctly');
            } else {
              console.log('❌ GitHub API access error:', res.statusCode);
              console.log('Response:', data);
            }
          });
        });
        
        req.on('error', (error) => {
          console.log('❌ Request error:', error.message);
        });
        
        req.end();
        EOF
        
        node verify_access.js
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

    - name: Commit and push if changed
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        if git diff --quiet && git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "🔄 Auto-update GitHub profile with enhanced stats [skip ci]

          ✨ Updates include:
          - Enhanced private repository statistics
          - Refreshed GitHub stats with cache-busting
          - Updated contribution activity graphs
          - Improved trophy and streak displays
          
          🔒 All statistics now properly include private repository contributions"
          git push
          echo "✅ Successfully pushed profile updates"
        fi

    - name: Create Summary
      run: |
        echo "## 📊 Profile Update Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### ✅ Completed Tasks:" >> $GITHUB_STEP_SUMMARY
        echo "- 🔄 Updated GitHub stats with private repository data" >> $GITHUB_STEP_SUMMARY
        echo "- 🏆 Refreshed GitHub trophies and achievements" >> $GITHUB_STEP_SUMMARY
        echo "- 📈 Updated contribution streak and activity graphs" >> $GITHUB_STEP_SUMMARY
        echo "- 🐍 Generated new GitHub contribution snake animation" >> $GITHUB_STEP_SUMMARY
        echo "- 🔒 Verified private repository access permissions" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🔧 Configuration:" >> $GITHUB_STEP_SUMMARY
        echo "- All stats include: \`count_private=true\` and \`include_all_commits=true\`" >> $GITHUB_STEP_SUMMARY
        echo "- Cache-busting timestamp: $(date +%s)" >> $GITHUB_STEP_SUMMARY
        echo "- Theme: Radical (dark mode optimized)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Next update:** $(date -d '+1 day' '+%Y-%m-%d at 00:00 UTC')" >> $GITHUB_STEP_SUMMARY
